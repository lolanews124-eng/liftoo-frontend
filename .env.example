VITE_API_URL=http://localhost:5000/api/v1
VITE_SOCKET_URL=http://localhost:5000

# Production (Vercel): set VITE_API_URL=https://api.liftoo.in/api/v1
# Geocoding/search goes through the backend — no Google API key needed in the frontend.
